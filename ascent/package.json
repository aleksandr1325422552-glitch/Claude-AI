{
  "name": "ascent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Ввысь — вертикальный платформер-раннер на Three.js: бесконечный подъём по башне от поднимающейся снизу пустоты",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "bundle": "vite build && node tools/build-single-file.mjs",
    "test": "node --test \"tests/**/*.test.js\"",
    "smoke": "vite build && node tools/smoke.mjs"
  },
  "license": "ISC",
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "playwright": "^1.62.0",
    "vite": "^8.1.5"
  }
}
