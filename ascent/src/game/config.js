/**
 * Настройки игры одним файлом.
 *
 * Ощущение платформера держится на десятке чисел, и они связаны: поднимешь
 * гравитацию — придётся поднять и силу прыжка, иначе герой перестанет
 * доставать до платформ, которые генератор ставит по этим же числам. Поэтому
 * всё, что влияет на баланс, лежит здесь, а не разбросано по системам.
 *
 * Единицы: метры, секунды, радианы.
 */

export const PHYSICS = {
  // Гравитация и сила прыжка подобраны так, чтобы апекс одиночного прыжка был
  // заметно выше TOWER.stepMax: базовый путь наверх обязан проходиться без
  // двойного прыжка, иначе подъём упирается в стамину и глохнет.
  gravity: -34,
  /** Падение тянет сильнее подъёма — прыжок ощущается «упругим», а не лунным. */
  fallGravityScale: 1.55,
  /** Пока держат прыжок и герой летит вверх, гравитация слабее. */
  holdGravityScale: 0.62,
  maxFallSpeed: -42,
  runSpeed: 11.5,
  airControl: 0.72,
  groundAccel: 68,
  airAccel: 34,
  groundFriction: 14,
  airDrag: 1.6,
  jumpSpeed: 17,
  /** Второй прыжок слабее первого — чтобы он не обесценивал точность первого. */
  doubleJumpSpeed: 14,
  /** Успеть прыгнуть после схода с края (сек). Классическое «время койота». */
  coyoteTime: 0.12,
  /** Нажатие прыжка запоминается на это время до приземления. */
  jumpBuffer: 0.14,
  dashSpeed: 26,
  dashDuration: 0.18,
  dashCooldown: 0.55,
  /** Радиус и высота капсулы героя. */
  radius: 0.42,
  height: 1.7,
  /** Максимальный уклон, по которому можно идти (косинус угла к вертикали). */
  maxSlope: 0.55,
  /** Ступенька, на которую герой заходит без прыжка. */
  stepHeight: 0.45,
}

export const STAMINA = {
  max: 100,
  /** Восстановление в секунду, когда герой стоит на земле. */
  regen: 26,
  /** На бегу восстанавливается медленнее — приходится выбирать момент. */
  regenMoving: 11,
  /** Пауза перед восстановлением после траты. */
  regenDelay: 0.45,
  dashCost: 22,
  doubleJumpCost: 18,
  /** Без стамины рывок и второй прыжок недоступны, но обычный прыжок — всегда. */
  wallGripDrain: 34,
}

export const HAZARD = {
  /**
   * Стартовое отставание пустоты от игрока.
   *
   * Запас щедрый намеренно: у пустоты огромная плоская поверхность, и стоя
   * ближе она занимает почти весь кадр — солнечное подножие превращается в
   * фиолетовую кашу, а первые секунды забега должны быть спокойными.
   */
  startGap: 70,
  /** Базовая скорость подъёма (м/с). */
  baseSpeed: 2.1,
  /** Прибавка к скорости за каждые 100 м высоты. */
  speedPerHundred: 0.22,
  maxSpeed: 9.5,
  /** Если игрок сильно оторвался, пустота подтягивается — забег не провисает. */
  catchUpDistance: 60,
  catchUpMultiplier: 2.6,
  /** Ближе этого расстояния экран начинает пульсировать. */
  warningDistance: 18,
}

export const TOWER = {
  /** Высота одного сегмента башни. */
  chunkHeight: 34,
  /** Сколько сегментов держим построенными выше игрока. */
  chunksAhead: 4,
  /** И сколько ниже — чтобы было куда падать и что видеть. */
  chunksBehind: 2,
  /** Радиус, в котором генератор расставляет платформы вокруг оси. */
  radiusMin: 7,
  radiusMax: 19,
  /**
   * Вертикальный шаг между платформами. Верхняя граница обязана оставаться
   * ниже апекса одиночного прыжка из PHYSICS — генератор проверяет это сам.
   */
  stepMin: 2.2,
  stepMax: 3.5,
  /** Чекпойнт каждые N метров. */
  checkpointEvery: 250,
}

export const ENEMY = {
  /** Преследователь: летит к игроку по прямой, огибая препятствия по высоте. */
  chaser: {
    speed: 7.4,
    acceleration: 9,
    /** Дальше этого расстояния теряет интерес и зависает. */
    aggroRange: 34,
    damageRadius: 1.15,
    /** Отбрасывание игрока при касании. */
    knockback: 14,
    health: 1,
  },
  /** Патрульный: ходит по платформе, сбивает при касании. */
  patrol: {
    speed: 3.2,
    turnPause: 0.6,
    damageRadius: 1.05,
    knockback: 12,
    health: 1,
  },
  /** Плевун: стоит на месте и стреляет снарядами вверх по дуге. */
  spitter: {
    fireInterval: 2.4,
    projectileSpeed: 15,
    projectileGravity: -14,
    aggroRange: 30,
    damageRadius: 0.8,
    knockback: 10,
    health: 1,
  },
  /** Насколько чаще враги встречаются на каждые 100 м высоты. */
  densityPerHundred: 0.09,
  maxDensity: 1.9,
}

export const PLAYER = {
  maxHealth: 3,
  /** Неуязвимость после удара. */
  invulnerable: 1.1,
  /** Падение ниже этой отметки от последней опоры считается провалом. */
  fallLimit: 45,
  respawnDelay: 0.9,
}

export const SCORE = {
  /** Очки за каждый метр рекордной высоты. */
  perMetre: 1,
  crystal: 25,
  checkpoint: 100,
  /** Множитель за серию без урона, растёт за каждый чекпойнт. */
  streakStep: 0.15,
  maxStreakMultiplier: 3,
}

export const PICKUP = {
  crystalRadius: 1.35,
  staminaRadius: 1.35,
  staminaRestore: 45,
  /** Сколько кристаллов в среднем на сегмент. */
  crystalsPerChunk: 2.4,
}
